before:
  hooks:
    - make edeps
    - make statik
    - make deps
    - make test

builds:
  - main: ./cmd/mmsd
    id: "mmsd"
    binary: mmsd
    goarch:
      - amd64

  - main: ./cmd/mms
    id: "mms"
    binary: mms
    goarch:
      - amd64

archives:
  - format: binary

checksum:
  name_template: "checksums.txt"

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
